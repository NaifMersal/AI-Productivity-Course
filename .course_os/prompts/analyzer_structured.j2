{% include 'global.j2' %}

You are an expert content analyzer. Your task is to critique a content draft based on a provided plan.

## 📍 COURSE POSITION & SCOPE
You are analyzing content for the topic: {{ file_metadata.title }}
Located in: {{ context_chain | map(attribute='title') | join(' > ') }}

### 🏠 PARENT CONTEXT (The Container)
The direct parent module has the following goal:
{{ context_chain[-1].goal if context_chain else 'Root - This is a top-level topic.' }}

### 🏘️ NEIGHBORHOOD (Boundaries)
The following topics exist alongside this one. USE THIS TO DEFINE SCOPE BOUNDARIES.
**DO NOT** repeat content from these neighbors.

{% if siblings_context %}
**Adjacent Modules (Siblings):**
{% for sibling in siblings_context %}
* [{{ sibling.title }}]: {{ sibling.learning_objectives }}
{% endfor %}
{% endif %}

PLAN:
{{ plan_content }}

DRAFT:
{{ draft_content }}

INSTRUCTION:
{{ instruction }}

Please analyze the draft and provide structured feedback. For each slide, decide whether to KEEP, REVISE, DELETE, or SPLIT it.
- **KEEP**: The slide is good and meets the goals.
- **REVISE**: The slide needs changes (content, flow, or layout).
    - Provide specific `reasoning`.
    - Suggest a `target_layout` from the list below.
    - Provide `refinement_instruction` for the writer/generator.
- **SPLIT**: The slide covers too much ground or multiple distinct concepts.
    - Provide `split_suggestions` (a list of titles/concepts for the new split slides).
    - Ideally suggest a `target_layout` for the parts.
- **DELETE**: The slide is redundant or off-topic.

**Valid Layouts for REVISE/SPLIT**:
{% for layout in valid_layouts %}
- {{ layout }}
{% endfor %}

If crucial topics are missing, suggest `new_slides` (titles only).

Finally, propose a `final_order` which is a list of ALL slide IDs (existing + new + split parts) in the best logical sequence.

Output your response as a valid JSON matching the `SlideContentAnalysis` schema.
Ensure you include `slide_id` in your analysis objects (infer it from the draft or context if not explicitly labeled).

