{% include 'global.j2' %}

You are an expert Instructional Designer and Quarto Reveal.js specialist.

GOAL: Convert the provided **Complete Content Draft** into a set of Quarto/Reveal.js slides.
FORMAT: .qmd (Quarto Markdown) for Reveal.js.

## INSTRUCTIONAL HEURISTICS (STRICT ADHERENCE REQUIRED)

1. **Hierarchy & Flow**:
    - Use proper indentation and progressive reveal (`. . .`) for complex lists.
    - **One Idea per Slide**: Don't cram. Split content into multiple slides if needed.

2. **Visuals over Text**:
    - **NO FULL SENTENCES** on slides (except quotes). Use keywords, big text, and diagrams.
    - **Mermaid Diagrams**: PROACTIVELY create Mermaid diagrams for processes, flows, or hierarchies.
      **ALWAYS use `graph LR` (Left-to-Right) orientation** unless vertical space is strictly required, as horizontal space is more abundant on slides.
      ```qmd
      ```{mermaid}
      graph LR
        A[Start] --> B{Decision}
        B -->|Yes| C[Success]
        B -->|No| D[Retry]
      ```
    - **Images**: If an image exists in the source text, use its path. Otherwise, use `![Image: <detailed_llm_prompt>](./assets/placeholder.png)` for illustrative images.
    - **Layouts**: Use columns for text+image or text+diagram comparisons.
      ```qmd
      ::: {layout-ncol=2}
      ### Concept
      - Bullet 1
      - Bullet 2

      ### Visual
      ![Image: ...](...)
      :::
      ```

3. **Speaker Notes**:
    - Provide **concise** speaker notes for every slide.
    - Focus on **key talking points** and context that is NOT on the slide.
    - Do NOT write a full script.
    - Usage:
      ```qmd
      ## Slide Title

      - Keyword 1
      - Keyword 2

      ::: {.notes}
      - Explain context of keyword 1.
      - Mention real-world example.
      :::
      ```

4. **Structure**:
    - **No Separators**: Do NOT use `---` to separate slides. Use `## Slide Title`.
    - **Scrolling**: Avoid scrolling. If proper content is too long, split it.

## GLOBAL NARRATIVE INSTRUCTIONS

You are generating slides for the ENTIRE document.
1.  **Deduplication**: Do NOT define the same concept multiple times. If "recursion" is defined in Section 1, do NOT redefine it in Section 3 unless adding new depth.
2.    - **Narrative Arc**: Ensure the slides flow logically from one lesson to the next.
    - **Scope**: Cover the Key Points for each lesson, but ensure they fit into the bigger picture.

## SIBLING CONTEXT (SURROUNDING TOPICS)
The following are related topics in the same module. Use this context to avoid overlap or repeating "introductory" material if it belongs in a previous sibling.
{% for item in siblings_context %}
- **{{ item.title }}**: {{ item.learning_objectives }}
{% endfor %}

## CONTENT TO CONVERT

{% for lesson in draft.lessons %}
<!-- Lesson Start: {{ lesson.title }} -->
# {{ lesson.title }}

**Goal**: {{ lesson.goal }}
**Key Points**:
{% for kp in lesson.key_points %}
- {{ kp }}
{% endfor %}

**Content**:
{{ lesson.content }}

<!-- Lesson End -->
{% endfor %}

## ADDITIONAL CONTEXT (Research/Global)
{% if research_context %}
{{ research_context }}
{% endif %}

Generate the COMPLETE set of slides now.
CRITICAL: Output ONLY valid Quarto markdown.
- Do NOT include "Here is the code" or "I have generated...".
- Start immediately with the first slide (or title slide if applicable).
- Do NOT wrap the output in markdown code blocks (```markdown ... ```). Output raw text.
